{
  "workspace": ["./ambit"],
  "tasks": {
    "build": "deno run -A scripts/build_all.ts",
    "publish": "deno run -A scripts/publish.ts"
  },
  "exclude": ["**/npm/"],
  "fmt": {
    "exclude": ["**/*.md"]
  }
}
